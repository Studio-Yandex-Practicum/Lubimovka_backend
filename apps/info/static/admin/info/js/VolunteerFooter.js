jQuery(document).ready(function ($) {
   //let divVolunteer = $(".model-volunteer current-model");
   var a = alert("Привет!");
   }
// #nav-sidebar > div.app-info.module.current-app > table > tbody > tr.model-volunteer.current-model
   a;
   alert("Привет!");
   //divVolunteer.scrollTop(0);
});
